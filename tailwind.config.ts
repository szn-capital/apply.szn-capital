import type { Config } from "tailwindcss";

export default {
    // Tailwind CSS v4 nutzt automatische Content-Detection
    // Theme-Konfiguration erfolgt in globals.css mit @theme
} satisfies Config;
